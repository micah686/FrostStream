[X]Playlists
upload metadata
Channel downloads
[X]audio downloads
live_chat (complex, need to integrate with clickhousedb)
[X]options passed through via api
background download channel assets (thumbnail, art) (maybe check cache as part of step)
batch local import (need to figure out if JUST local filesystem, or use IFluentStorage)
    Implement complete offline media file import from json file
backup and restore (should be postgres+openbao+...)
Dynamic download queue 


TubeArchivst options
Search download queue
Configure shorts, streams and video sizes per channel 
User created playlists 
User roles, aka read only user (hook into auth framework/APIs)
Add statistics of index 
Implement  notifications 
Download video comments
Show similar videos on video page
Implement complete offline media file import from json file
Filter and query in search form, search by url query
Make items in grid row configurable to use more of the screen 
Add passing browser cookies to yt-dlp 
Add SponsorBlock integration 
Implement per channel settings 
Subtitle download & indexing 
Fancy advanced unified search interface 
Auto rescan and auto download on a schedule 
Optional automatic deletion of watched items after a specified time 
Delete videos and channel (probably be global vs a specific storage key)
Add thumbnail embed option 
Un-ignore videos 
